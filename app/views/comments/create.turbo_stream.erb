<%= turbo_stream.update 'comment_form', partial: 'comments/form', locals: { post: @comment.post, comment: Comment.new } %>
<%= turbo_stream.update 'comments', partial: 'comments/comments', locals: { comments: @comment.post.comments.includes(:user).order(created_at: :desc) } %>