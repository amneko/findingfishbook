<section class="section">
  <div class="container">
    <div class="columns is-centered">
      <div class="column is-three-quarters">
        <h1 class="title">ログイン</h1>
        <%= form_with url: login_path, local: true, class: 'form' do |f| %>
          <div class="field">
            <%= f.label :email, class: 'label' %>
            <div class="control">
              <%= f.text_field :email, class: 'input' %>
            </div>
          </div>
          <div class="field">
            <%= f.label :password, class: 'label' %>
            <div class="control">
              <%= f.password_field :password, class: 'input' %>
            </div>
          </div>
          <div class="field">
            <div class="control">
              <%= f.submit "ログイン", class: 'button is-primary' %>
            </div>
          </div>
        <% end %>
        <div class="has-text-centered">
          <%= link_to "登録ページへ", new_user_path, class: 'has-text-primary' %>
          <%# <%= link_to "パスワードをお忘れの方はこちら", new_password_reset_path, class: 'has-text-primary' %>
        </div>
      </div>
    </div>
  </div>
</section>
